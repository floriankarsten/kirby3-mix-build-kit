module.exports = {
        // name of main folder that you can deploy
        projectFolder: "project",

        // name of public folder if you use one
        publicFolder: "public",

        // url to browsersync proxy (depending on your webserver)
        proxy: "mix.local"
}